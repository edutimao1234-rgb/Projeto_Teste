# Projeto Teste (Estrutura Profissional Python)

Projeto base em Python utilizando:

- Layout `src/`
- Pytest para testes automatizados
- Ruff para lint
- Black para formatação
- Git com versionamento limpo

## 📁 Estrutura
Projeto_Teste/
├── src/
│ └── projeto_teste/
│ ├── init.py
│ └── main.py
├── tests/
├── pyproject.toml
├── .gitignore

## ▶️ Como rodar

Criar ambiente virtual:
python -m venv .venv

Ativar (Git Bash):
source .venv/Scripts/activate

Instalar dependências:
pip install -U pip
pip install pytest ruff black

Rodar testes:
pytest

## 🧪 Testes

O projeto utiliza `pytest` para validação automática.

## 🎯 Objetivo

Este projeto faz parte do plano de desenvolvimento profissional em Python, focado em:

- Estrutura de código profissional
- Testes automatizados
- Boas práticas
- Preparação para o mercado
